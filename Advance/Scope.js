// console.log(name);
var name = "ABC";

if (true) {
  let lastName = "XYZ";
}

/**
 * It will print the output
 * To alot of programmer it is not a good things
 * what the habit we have develop is variable which are define inside the
 *  scope should go away and shouldn't accessable to the outside scope
 */
console.log(lastName);
