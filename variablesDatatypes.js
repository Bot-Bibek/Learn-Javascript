//Lets Reserve some memory
//var,let and const

var FullName = "ABC"; //this is a varibales so i have resever some memory

//how to name variables
//while defining the variables i preferred the camelCase (FullName, fullName)

var courseName = "JavaScripts"; //String Value

var isLoggedIn = true; //Boolean Value

var loggedCount = 34; //Number Value
// console.log(loggedCount)

var paymentMode; //Undefined Value
paymentMode = "Credit Card";
console.log(paymentMode);

//These are the datatypes of JS well there is many datatypes but these are the main datatypes that must be remember.
//Also there are a reserve word like "console" and many more's 


